# План рефакторинга и оптимизации Flutter 2048

Детальный план действий по исправлению ошибок, оптимизации производительности, улучшению UX/UI и написанию тестов для приложения 2048 на Flutter.

---

## 1. Архитектура и Виджеты UI

- **[board_grid.dart](file:///c:/Projects/flutter_2048/lib/views/widgets/board_grid.dart)**:
  - [ ] Преобразовать `BoardGrid` в `StatefulWidget`.
  - [ ] Вынести `FocusNode` в `initState()` и добавить корректный `dispose()`, чтобы устранить утечку памяти.
  - [ ] Заменить устаревший `RawKeyboardListener` на `KeyboardListener`.
  - [ ] Переписать обработку свайпов в `GestureDetector`: использовать накопленное смещение (`dx`/`dy`), а не только конечную скорость отпускания.

- **[game_screen.dart](file:///c:/Projects/flutter_2048/lib/views/game_screen.dart)**:
  - [ ] Убрать метод `_onStateChange`, вызывающий полный `setState()` всего экрана при каждом движении.
  - [ ] Обернуть изменяемые компоненты (`ScoreBox`, `BoardGrid`, `GameOverOverlay`) в локальные `ListenableBuilder` / `AnimatedBuilder`.

---

## 2. Игровая Логика и Производительность

- **[game_controller.dart](file:///c:/Projects/flutter_2048/lib/controllers/game_controller.dart)**:
  - [ ] Убрать антипаттерн `try-catch` из метода `_getTileAt`. Использовать стандартную проверку коллекции или `firstWhereOrNull`.
  - [ ] Исправить баг слияния: не удалять плитки мгновенно, а дожидаться окончания визуальной анимации смещения перед их удалением из структуры данных.
  - [ ] Реализовать автосохранение текущей матрицы плиток и счета в `SharedPreferences`, чтобы прогресс игры не терялся при выходе.
  - [ ] Разделить логику темы и игры (вынести тему из `GameController` в отдельный менеджер).

---

## 3. UX и Геймплей

- [ ] Добавить экран/диалог выбора продолжения игры ("Keep Playing") после достижения плитки 2048.
- [ ] Разработать функцию отмены последнего хода (Undo).
- [ ] Добавить визуальную анимацию прибавления очков (всплывающий "+8", "+16" возле счета).

---

## 4. Тестирование

- **[game_controller_test.dart](file:///c:/Projects/flutter_2048/test/game_controller_test.dart)**:
  - [ ] Написать Unit-тесты для метода `move()` во всех 4 направлениях (Up, Down, Left, Right).
  - [ ] Протестировать граничные случаи слияний (например, цепочка `[2, 2, 2, 2]` или `[4, 4, 8, 16]`).
  - [ ] Протестировать алгоритм определения окончания игры `_checkGameOver()`.

- **[widget_test.dart](file:///c:/Projects/flutter_2048/test/widget_test.dart)**:
  - [ ] Добавить UI-тесты взаимодействия с кнопками "New Game", смены темы и показа оверлея победы/проигрыша.

---

## 5. Проверка и Валидация

```bash
flutter analyze
flutter test
```
